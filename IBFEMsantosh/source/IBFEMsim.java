//import onsim.DeskSim;
import Interpolation.*;
//==============================================================================
//      IBFEMsim CLASS
//==============================================================================
public class IBFEMsim {

    //==========================================================================
    //                Main
    //==========================================================================
    /* To debug: 
    *  (i) Set DEBUG = true in the main program below
    *  (ii) Run Project and wait for message on output window, then
    *  (iii) Debug->Attach Debugger.. and set Port = 8888
    *  If source incorrect check: Window->Debugging->Sources
    *     and turn off any duplicate sources that are not needed
     */
    public static void main(String[] args) {
        /*
        boolean DEBUG = false;
        // Download code
        String SharedFolder = "../../SharedFiles/";
        DeskSim deskSim = new DeskSim(SharedFolder);
        if (DEBUG) {
            deskSim.debug("IBFEM");
        } else {
            deskSim.launch("IBFEM");
        }
         */
        
      
        
    }

} // IBFEMsim

